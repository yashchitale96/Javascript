"use strict" // treat all JS code as a newer version

// alert(3+3) // it gives error because we are using node js and not browser

console.log(3 
    + 3 ) // it will gives output but bad practice

let name = "Yash"
let age = 18
let isLoggedIn = false
let state;

/*
    DataTypes
    1.Number = 2 to power 53
    2.bigint
    string => ""
    boolean => true/false
    null => standalone value
    undefined =>
    symbol => unique
*/

console.log(typeof "Yash");
console.log(typeof undefined); // undefined
console.log(typeof null); // object

